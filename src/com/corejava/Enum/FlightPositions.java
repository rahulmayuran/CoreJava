package com.corejava.Enum;

/*We can use enum type for defining a type with finite list of valid 
 * values, 
1. Declare with enum keyword
2. provide a comma seperated list*/

public enum FlightPositions {
    Pilot,
    CoPilot,
    FlightAttendant,
    AirMarshal,
    AirHostress
}
